---
import Base from "../layouts/Base.astro";
import { renderMarkdown, readDoc } from "../lib/standard.js";

// h1 уже нарисован страницей, состав реестров описан в самом README
const body = renderMarkdown(readDoc("appendix", "README.md"), "appendix", ["h1"]);
---

<Base
  title="Приложение: как стандарт проверялся"
  description="166 тарифных вариантов из реальных прайс-листов операторов, разобранных в термины FSP: проверки валидатора и находки, закрытые правками модулей. Не входит в стандарт."
>
  <p class="crumbs"><a href="/">FSP</a> / Приложение</p>
  <h1>Приложение: как стандарт проверялся</h1>

  <div class="prose" set:html={body} />
</Base>
